{
  "name": "@my-binder/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp -r src/db/migrations dist/db/",
    "dev": "tsx watch index.ts",
    "start": "node dist/index.js",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@duckdb/node-api": "latest",
    "@my-binder/core": "workspace:*",
    "fastify": "^4.29.0",
    "mtgjson-sdk": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
