// Spec 022 — public barrel for the Card Scanner feature (Principle IX). Pure
// re-export: the only consumer-facing surface is the Container, mounted by the
// `scan/index` route in the scan tab stack.
export { CardScannerContainer } from './CardScannerContainer';
