// Spec 020 — public barrel for the Card Detail Sheet feature (Principle IX).
// Pure re-export: the only consumer-facing surface is the Container, mounted by
// the `card-detail` form-sheet route on both the Catalogue and Binder stacks.
export { default as CardDetailSheetContainer } from './CardDetailSheetContainer';
