// Public surface for the catalogue context (Principle IX — barrel re-export only).
export { CatalogueProvider } from './CatalogueProvider';
export { useCatalogueContext } from './useCatalogueContext';
export type { CatalogueContextValue } from './types';
